as always all parts should be run by navigating to the inner file and simply entering make, and ./main. Additonally part1plswork was used for graph generation, in order for it to generate a graph the main.cpp must be open and the corresponding boolean value switched to true position, it is the authors suggestion you only attempt to build one graph at a time. Text files needed to produce the graphs are included as well. 